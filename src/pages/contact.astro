---
import { getEntry, render } from "astro:content";
import { Icon } from "astro-icon/components";
import Markdown from "@components/misc/Markdown.astro";
import I18nKey from "../i18n/i18nKey";
import { i18n } from "../i18n/translation";
import MainGridLayout from "../layouts/MainGridLayout.astro";

const contactPage = await getEntry("spec", "contact");

if (!contactPage) {
    throw new Error("Contact page content not found");
}

const { Content } = await render(contactPage);
const contactFieldClass =
    "contact-field w-full text-sm text-black/60 dark:text-white/60 placeholder:text-black/30 dark:placeholder:text-white/30";
---
<MainGridLayout title={i18n(I18nKey.contact)} description={i18n(I18nKey.contact)} disableEntranceAnimations={true}>
    <div class="w-full flex flex-col gap-4">
        <div class="flex w-full rounded-[var(--radius-large)] overflow-hidden relative min-h-32">
            <div class="card-base z-10 px-9 pb-6 relative w-full">
                <Markdown class="mt-2">
                    <Content />
                </Markdown>
            </div>
        </div>

        <!-- Header -->
        <div class="card-base px-6 py-6 text-center">
            <h1 class="text-3xl md:text-4xl font-bold text-[var(--primary)]" data-i18n="contactTitle">Contact Us</h1>
        </div>

        <!-- Info Boxes Grid -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">

            <!-- Email -->
            <div class="card-base p-5 flex flex-col items-center text-center gap-3 group hover:shadow-lg transition-shadow">
                <div class="w-12 h-12 rounded-2xl bg-[var(--btn-regular-bg)] group-hover:bg-[var(--primary)] flex items-center justify-center transition-colors">
                    <Icon name="material-symbols:mail-outline-rounded" class="w-5 h-5 text-[var(--btn-content)] group-hover:text-white transition-colors" />
                </div>
                <h3 class="font-semibold text-sm text-50 uppercase tracking-wider" data-i18n="contactEmail">Email</h3>
                <a href="mailto:info@tadawilifetech.com" class="text-[var(--primary)] hover:underline text-sm break-all" dir="ltr">info@tadawilifetech.com</a>
            </div>

            <!-- Phone -->
            <div class="card-base p-5 flex flex-col items-center text-center gap-3 group hover:shadow-lg transition-shadow">
                <div class="w-12 h-12 rounded-2xl bg-[var(--btn-regular-bg)] group-hover:bg-[var(--primary)] flex items-center justify-center transition-colors">
                    <Icon name="material-symbols:call-outline-rounded" class="w-5 h-5 text-[var(--btn-content)] group-hover:text-white transition-colors" />
                </div>
                <h3 class="font-semibold text-sm text-50 uppercase tracking-wider" data-i18n="contactPhone">Phone</h3>
                <a href="tel:+96891235160" class="text-[var(--primary)] hover:underline text-sm" dir="ltr">+968 91235160</a>
            </div>

            <!-- Working Hours -->
            <div class="card-base p-5 flex flex-col items-center text-center gap-3 group hover:shadow-lg transition-shadow">
                <div class="w-12 h-12 rounded-2xl bg-[var(--btn-regular-bg)] group-hover:bg-[var(--primary)] flex items-center justify-center transition-colors">
                    <Icon name="material-symbols:schedule-outline-rounded" class="w-5 h-5 text-[var(--btn-content)] group-hover:text-white transition-colors" />
                </div>
                <h3 class="font-semibold text-sm text-50 uppercase tracking-wider" data-i18n="contactHours">Working Hours</h3>
                <p class="text-90 text-sm" dir="ltr">9:00 – 13:00 &amp; 16:00 – 19:00</p>
            </div>

            <!-- Instagram -->
            <div class="card-base p-5 flex flex-col items-center text-center gap-3 group hover:shadow-lg transition-shadow">
                <div class="w-12 h-12 rounded-2xl bg-[var(--btn-regular-bg)] group-hover:bg-[var(--primary)] flex items-center justify-center transition-colors">
                    <Icon name="fa6-brands:instagram" class="w-5 h-5 text-[var(--btn-content)] group-hover:text-white transition-colors" />
                </div>
                <h3 class="font-semibold text-sm text-50 uppercase tracking-wider" data-i18n="contactInstagram">Instagram</h3>
                <div class="flex flex-col gap-1">
                    <a href="https://instagram.com/Ortino_arabic" target="_blank" rel="noopener noreferrer" class="text-[var(--primary)] hover:underline text-sm" dir="ltr">@Ortino_arabic</a>
                    <a href="https://instagram.com/sleeptimecare.oman" target="_blank" rel="noopener noreferrer" class="text-[var(--primary)] hover:underline text-sm" dir="ltr">@sleeptimecare.oman</a>
                </div>
            </div>

        </div>

        <!-- Address Full Width -->
        <div class="card-base p-5 flex items-center gap-4 group hover:shadow-lg transition-shadow">
            <div class="flex-shrink-0 w-12 h-12 rounded-2xl bg-[var(--btn-regular-bg)] group-hover:bg-[var(--primary)] flex items-center justify-center transition-colors">
                <Icon name="material-symbols:location-on-outline-rounded" class="w-5 h-5 text-[var(--btn-content)] group-hover:text-white transition-colors" />
            </div>
            <div>
                <h3 class="font-semibold text-sm text-50 uppercase tracking-wider mb-0.5" data-i18n="contactAddress">Address</h3>
                <p class="text-90">Al Ghubrah, Muscat, Oman</p>
            </div>
        </div>

        <!-- Contact Form -->
        <div class="card-base px-6 md:px-10 py-8 md:py-10">
            <h2 class="text-xl font-bold text-90 mb-2" data-i18n="contactFormTitle">Send us a message</h2>
            <p class="text-50 text-sm mb-6" data-i18n="contactFormLead">Use the same quick contact flow as the site search: simple, clear, and direct.</p>
            <form id="contact-form" class="flex flex-col gap-5">
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-5">
                    <div>
                        <label for="cf-name" class="block text-sm font-medium text-75 mb-1.5" data-i18n="contactName">Full Name</label>
                        <div class="contact-input-shell group">
                            <Icon name="material-symbols:person-outline-rounded" class="contact-icon w-5 h-5" />
                            <input type="text" id="cf-name" name="name" required class={contactFieldClass + " contact-field-with-icon"}
                                data-i18n-placeholder="contactNamePh" placeholder="Your name" />
                        </div>
                    </div>
                    <div>
                        <label for="cf-email" class="block text-sm font-medium text-75 mb-1.5" data-i18n="contactEmailLabel">Email</label>
                        <div class="contact-input-shell group">
                            <Icon name="material-symbols:mail-outline-rounded" class="contact-icon w-5 h-5" />
                            <input type="email" id="cf-email" name="email" required class={contactFieldClass + " contact-field-with-icon"}
                                dir="ltr" placeholder="your@email.com" />
                        </div>
                    </div>
                </div>
                <div>
                    <label for="cf-phone" class="block text-sm font-medium text-75 mb-1.5" data-i18n="contactPhoneLabel">Phone Number</label>
                    <div class="contact-input-shell group">
                        <Icon name="material-symbols:call-outline-rounded" class="contact-icon w-5 h-5" />
                        <input type="tel" id="cf-phone" name="phone" class={contactFieldClass + " contact-field-with-icon"}
                            dir="ltr" placeholder="+968 ..." />
                    </div>
                </div>
                <div>
                    <label for="cf-subject" class="block text-sm font-medium text-75 mb-1.5" data-i18n="contactSubject">Subject</label>
                    <div class="contact-input-shell group">
                        <Icon name="material-symbols:chat-outline-rounded" class="contact-icon w-5 h-5" />
                        <input type="text" id="cf-subject" name="subject" required class={contactFieldClass + " contact-field-with-icon"}
                            data-i18n-placeholder="contactSubjectPh" placeholder="How can we help?" />
                    </div>
                </div>
                <div>
                    <label for="cf-message" class="block text-sm font-medium text-75 mb-1.5" data-i18n="contactMessage">Message</label>
                    <div class="contact-input-shell group contact-textarea-shell">
                        <Icon name="material-symbols:edit-note-rounded" class="contact-icon contact-icon-top w-5 h-5" />
                        <textarea id="cf-message" name="message" rows="5" required
                            class={contactFieldClass + " contact-field-with-icon min-h-[9rem] resize-y py-4"}
                            data-i18n-placeholder="contactMessagePh" placeholder="Write your message..."></textarea>
                    </div>
                </div>
                <button type="submit" id="cf-submit"
                    class="self-start px-8 py-3 rounded-xl bg-[var(--primary)] text-white font-semibold text-base hover:opacity-90 active:scale-[0.98] transition-all duration-150 flex items-center gap-2">
                    <Icon name="fa6-brands:whatsapp" class="w-5 h-5" />
                    <span data-i18n="contactSend">Send via WhatsApp</span>
                </button>
            </form>
        </div>

    </div>

    <script is:inline>
        (function() {
            var translations = {
                en: {
                    contactTitle: 'Contact Us',
                    contactEmail: 'Email',
                    contactPhone: 'Phone',
                    contactHours: 'Working Hours',
                    contactInstagram: 'Instagram',
                    contactAddress: 'Address',
                    contactFormTitle: 'Send us a message',
                    contactFormLead: 'Use the same quick contact flow as the site search: simple, clear, and direct.',
                    contactName: 'Full Name',
                    contactNamePh: 'Your name',
                    contactEmailLabel: 'Email',
                    contactPhoneLabel: 'Phone Number',
                    contactSubject: 'Subject',
                    contactSubjectPh: 'How can we help?',
                    contactMessage: 'Message',
                    contactMessagePh: 'Write your message...',
                    contactSend: 'Send via WhatsApp'
                },
                ar: {
                    contactTitle: 'اتصل بنا',
                    contactEmail: 'البريد الإلكتروني',
                    contactPhone: 'الهاتف',
                    contactHours: 'ساعات العمل',
                    contactInstagram: 'انستغرام',
                    contactAddress: 'العنوان',
                    contactFormTitle: 'أرسل لنا رسالة',
                    contactFormLead: 'نفس أسلوب البحث في الموقع: سريع وواضح ومباشر.',
                    contactName: 'الاسم الكامل',
                    contactNamePh: 'اسمك',
                    contactEmailLabel: 'البريد الإلكتروني',
                    contactPhoneLabel: 'رقم الهاتف',
                    contactSubject: 'الموضوع',
                    contactSubjectPh: 'كيف يمكننا مساعدتك؟',
                    contactMessage: 'الرسالة',
                    contactMessagePh: 'اكتب رسالتك...',
                    contactSend: 'أرسل عبر واتساب'
                }
            };

            function applyContactTranslations() {
                var lang = localStorage.getItem('lang') || 'en';
                var t = translations[lang] || translations.en;
                document.querySelectorAll('[data-i18n]').forEach(function(el) {
                    var key = el.getAttribute('data-i18n');
                    if (t[key]) el.textContent = t[key];
                });
                document.querySelectorAll('[data-i18n-placeholder]').forEach(function(el) {
                    var key = el.getAttribute('data-i18n-placeholder');
                    if (t[key]) el.setAttribute('placeholder', t[key]);
                });
            }

            applyContactTranslations();
            window.addEventListener('storage', function(e) {
                if (e.key === 'lang') applyContactTranslations();
            });
            var langObserver = new MutationObserver(function() { applyContactTranslations(); });
            langObserver.observe(document.documentElement, { attributes: true, attributeFilter: ['lang'] });
        })();

        document.getElementById('contact-form').addEventListener('submit', function(e) {
            e.preventDefault();
            var name = document.getElementById('cf-name').value.trim();
            var email = document.getElementById('cf-email').value.trim();
            var phone = document.getElementById('cf-phone').value.trim();
            var subject = document.getElementById('cf-subject').value.trim();
            var message = document.getElementById('cf-message').value.trim();

            var text = '*New Contact Message*\n\n'
                + '*Name:* ' + name + '\n'
                + '*Email:* ' + email + '\n'
                + (phone ? '*Phone:* ' + phone + '\n' : '')
                + '*Subject:* ' + subject + '\n\n'
                + '*Message:*\n' + message;

            var whatsappUrl = 'https://api.whatsapp.com/send?phone=96891235160&text=' + encodeURIComponent(text);
            window.open(whatsappUrl, '_blank', 'noopener,noreferrer');
        });
    </script>

    <style is:inline>
        .contact-input-shell {
            position: relative;
            display: flex;
            align-items: center;
            min-height: 3.25rem;
            border-radius: 1rem;
            background: rgba(0, 0, 0, 0.04);
            transition: background-color 160ms ease, box-shadow 160ms ease, transform 160ms ease;
        }

        :root.dark .contact-input-shell {
            background: rgba(255, 255, 255, 0.05);
        }

        .contact-input-shell:hover,
        .contact-input-shell:focus-within {
            background: rgba(0, 0, 0, 0.06);
        }

        :root.dark .contact-input-shell:hover,
        :root.dark .contact-input-shell:focus-within {
            background: rgba(255, 255, 255, 0.1);
        }

        .contact-input-shell:focus-within {
            box-shadow: 0 0 0 1px rgba(0, 0, 0, 0.06), 0 0 0 3px color-mix(in srgb, var(--primary) 22%, transparent);
            transform: translateY(-1px);
        }

        :root.dark .contact-input-shell:focus-within {
            box-shadow: 0 0 0 1px rgba(255, 255, 255, 0.08), 0 0 0 3px color-mix(in srgb, var(--primary) 24%, transparent);
        }

        .contact-field {
            width: 100%;
            border: 0;
            background: transparent;
            outline: 0;
            padding-top: 0.95rem;
            padding-bottom: 0.95rem;
            padding-right: 1rem;
            padding-left: 1rem;
        }

        .contact-field-with-icon {
            padding-inline-start: 3rem;
        }

        .contact-icon {
            pointer-events: none;
            position: absolute;
            inset-inline-start: 1rem;
            top: 50%;
            z-index: 1;
            transform: translateY(-50%);
            color: rgba(0, 0, 0, 0.3);
            transition: color 160ms ease;
        }

        :root.dark .contact-icon {
            color: rgba(255, 255, 255, 0.3);
        }

        .contact-icon-top {
            top: 1rem;
            transform: none;
        }

        .contact-textarea-shell {
            align-items: flex-start;
        }

        .contact-textarea-shell .contact-field {
            line-height: 1.7;
        }

        .contact-input-shell:focus-within svg {
            color: var(--primary);
        }
    </style>
</MainGridLayout>
